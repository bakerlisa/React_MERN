import React from 'react';
import axios from 'axios';

const Home = (props) => {
    return(
        <div>
            <h1>Where do you Call Home?</h1>
            <p> Go where no one's has ever gone before! </p>
        </div>
    );
}

export default Home;