import React from 'react';

const RunningDisplay = (props) =>{
    return(
        <div>
            <h1>Test</h1>
            <p>{props.upevent.distance}</p>
        </div>
    );
}

export default RunningDisplay;